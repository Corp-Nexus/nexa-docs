{
  "name": "nexa-docs",
  "version": "1.0.0",
  "description": "Documentação da plataforma Nexa - Site estático servido via http-server",
  "main": "intro/index.html",
  "scripts": {
    "start": "http-server . -p 3010 -a 0.0.0.0 --cors -c-1 -d false",
    "dev": "http-server . -p 3010 -a 0.0.0.0 --cors -c-1 -d false -o",
    "build": "echo 'Site estático - nenhum build necessário'",
    "test": "echo 'Nenhum teste configurado'"
  },
  "keywords": [
    "documentation",
    "nexa",
    "static-site",
    "whatsapp-automation"
  ],
  "author": "CorpNexus",
  "license": "MIT",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}